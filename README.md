# smart-log-incident-detection
Pure-Python server log pipeline that validates, cleans, and classifies raw log entries by severity — detecting incidents and generating structured incident summaries with full error handling.
